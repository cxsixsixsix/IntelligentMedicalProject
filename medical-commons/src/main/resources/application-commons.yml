spring:
  ########## 配置开启Spring Boot 对于bean id 的Override机制
  main:
    allow-bean-definition-overriding: true
  ########## 配置连接注册中心 Nacos 信息
  cloud:
    nacos:
      discovery:
        server-addr: http://127.0.0.1:8848